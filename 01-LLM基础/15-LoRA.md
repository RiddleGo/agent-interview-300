# 第 15 题：LoRA（Low-Rank Adaptation）的数学原理

## 题目

解释 LoRA（Low-Rank Adaptation）微调的数学原理。

---

## 一、动机

全量微调大模型要更新全部参数，显存与算力成本高。**LoRA** 假设预训练权重在适应新任务时，**有效更新是低秩的**，因此只学习一个低秩增量，与原权重相加得到适应后的权重，从而大幅减少可训练参数与显存。

---

## 二、数学形式

设某层线性变换为 $\mathbf{h} = W \mathbf{x}$，$W \in \mathbb{R}^{d \times k}$。LoRA 不直接改 $W$，而是增加**低秩分解**的增量：

$$
W' = W + \Delta W = W + A B
$$

其中 $A \in \mathbb{R}^{d \times r}$，$B \in \mathbb{R}^{r \times k}$，$r \ll \min(d,k)$ 为秩。前向时：

$$
\mathbf{h} = W\mathbf{x} + (AB)\mathbf{x} = W\mathbf{x} + A(B\mathbf{x})
$$

- **训练**：只训练 $A$、$B$，$W$ 冻结；参数量从 $d \times k$ 变为 $r(d+k)$。  
- **推理**：可把 $W' = W + AB$ 算好存成一张矩阵，等价于单层线性，无额外延迟；或保留 $W + AB$ 分开算以省显存（只存一份 $W$ 多份 $A,B$）。

---

## 三、设计要点

- **初始化**：通常 $A$ 用高斯小随机，$B$ 用零初始化，使训练初态 $\Delta W = 0$，不破坏预训练表示。  
- **秩 $r$**：$r$ 越大表达能力越强、参数越多；常见 8、16、32，在多数任务上小秩即可接近全量微调。  
- **放置位置**：Transformer 中通常对 **attention 的 Q/V（或 Q/K/V）** 以及 **FFN 的某层** 加 LoRA，其余冻结。

**面试常问**：“为什么 B 零初始化？”——这样训练开始时 $\Delta W = AB = 0$，不破坏预训练表示，从“无增量”开始学。 “秩 r 怎么选？”——常用 8、16、32；r 越大表达能力越强但参数越多，多数任务小秩就够。 “推理时要算两次线性吗？”——可以先把 $W' = W + AB$ 算好存起来，推理就和普通线性层一样；也可以只存 $W$，多份 $A,B$ 按任务切换，省显存。

---

## 四、小结与面试要点

**小结**：LoRA 用 $W' = W + AB$ 表示低秩增量，只训 $A,B$，冻结 $W$；参数量 $r(d+k) \ll dk$，省显存与算力；推理可合并为 $W'$ 或保留多组 $A,B$ 做多任务。

**面试要点**：公式 $W'=W+AB$、只训 A/B、初始化（A 小随机 B 零）、放置位置（Q/V 或 Q/K/V、FFN）、推理可合并。

---

## 记忆要点

1. LoRA：$W' = W + AB$，A∈R^{d×r}，B∈R^{r×k}；只训 A、B，冻结 W。  
2. 初始化：A 小随机，B 零，使初始 ΔW=0。  
3. 参数量：r(d+k) ≪ dk；推理可合并为 W' 或多组 A,B 切换。

---

*上一篇：[第 14 题 - 梯度累积](./14-梯度累积.md)*  
*下一篇：[第 16 题 - 微调方法对比](./16-微调方法对比.md)*
